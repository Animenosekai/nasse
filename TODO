TOP PRIORITY

- Manage multiple headers with same name in the same section for docs

TODO

- Writing Tests
- Automatic `Return` values
- `Raises` in `miko` docs to `nasse` errors
- Automatic object definition part in docs, avoiding built-ins and defining objects with their `__doc__`
